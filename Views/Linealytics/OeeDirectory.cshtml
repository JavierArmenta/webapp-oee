@{
    ViewData["Title"] = "Directorio OEE - Linealytics";
}

<style>
    /* Hero */
    .lnx-hero {
        background: linear-gradient(135deg, #1e3a5f 0%, #2563a8 100%);
        border-radius: 0.375rem;
        padding: 2.5rem 2rem;
        color: #fff;
        position: relative;
        overflow: hidden;
        margin-bottom: 1.75rem;
    }

    .lnx-hero::after {
        content: '';
        position: absolute;
        right: -40px;
        top: -40px;
        width: 220px;
        height: 220px;
        background: rgba(255, 255, 255, 0.04);
        border-radius: 50%;
    }

    .lnx-hero::before {
        content: '';
        position: absolute;
        right: 60px;
        bottom: -60px;
        width: 160px;
        height: 160px;
        background: rgba(255, 255, 255, 0.06);
        border-radius: 50%;
    }

    .lnx-hero h1 {
        font-size: 1.75rem;
        font-weight: 700;
        margin-bottom: 0.4rem;
    }

    .lnx-hero p {
        opacity: 0.8;
        margin-bottom: 0;
        font-size: 0.9375rem;
    }

    .lnx-hero .hero-icon {
        font-size: 5rem;
        opacity: 0.12;
        position: absolute;
        right: 2rem;
        top: 50%;
        transform: translateY(-50%);
        line-height: 1;
    }

    /* Section label */
    .lnx-section-label {
        display: flex;
        align-items: center;
        gap: 0.6rem;
        font-size: 0.7rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        margin-bottom: 1rem;
        color: #64748b;
    }

    .lnx-section-label::after {
        content: '';
        flex: 1;
        height: 1px;
        background: #e2e8f0;
    }

    .lnx-section-label .badge-label {
        background: #f1f5f9;
        border-radius: 99px;
        padding: 0.25rem 0.75rem;
        display: flex;
        align-items: center;
        gap: 0.4rem;
    }

    /* Module cards */
    .lnx-card {
        border: 1px solid #e9ecef;
        border-radius: 0.5rem;
        background: #fff;
        height: 100%;
        transition: box-shadow 0.2s ease, transform 0.2s ease, border-color 0.2s ease;
        overflow: hidden;
        display: flex;
        flex-direction: column;
    }

    .lnx-card:hover {
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        transform: translateY(-3px);
        border-color: transparent;
    }

    .lnx-card-top {
        padding: 1.25rem 1.25rem 0;
        display: flex;
        align-items: flex-start;
        gap: 1rem;
    }

    .lnx-icon-box {
        width: 2.75rem;
        height: 2.75rem;
        border-radius: 0.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        font-size: 1.3rem;
    }

    .lnx-card-title {
        font-weight: 600;
        font-size: 0.9375rem;
        color: #1e293b;
        margin-bottom: 0.2rem;
        line-height: 1.3;
    }

    .lnx-card-desc {
        font-size: 0.8125rem;
        color: #94a3b8;
        line-height: 1.5;
    }

    .lnx-card-footer {
        padding: 1rem 1.25rem 1.25rem;
        margin-top: auto;
    }

    .lnx-card-footer a {
        font-size: 0.8125rem;
        font-weight: 500;
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        text-decoration: none;
        transition: gap 0.2s;
    }

    .lnx-card-footer a:hover {
        gap: 0.6rem;
    }

    /* Color variants */
    .lnx-icon-blue   { background: #eff6ff; color: #2563eb; }
    .lnx-icon-red    { background: #fff1f2; color: #e11d48; }
    .lnx-icon-teal   { background: #f0fdfa; color: #0d9488; }
    .lnx-icon-slate  { background: #f8fafc; color: #475569; }
    .lnx-icon-amber  { background: #fffbeb; color: #d97706; }
    .lnx-icon-green  { background: #f0fdf4; color: #16a34a; }
    .lnx-icon-purple { background: #faf5ff; color: #7c3aed; }
    .lnx-icon-orange { background: #fff7ed; color: #ea580c; }

    .lnx-link-blue   { color: #2563eb; }
    .lnx-link-red    { color: #e11d48; }
    .lnx-link-teal   { color: #0d9488; }
    .lnx-link-slate  { color: #475569; }
    .lnx-link-amber  { color: #d97706; }
    .lnx-link-green  { color: #16a34a; }
    .lnx-link-purple { color: #7c3aed; }
    .lnx-link-orange { color: #ea580c; }
</style>

<div class="container-fluid">

    <!-- Hero -->
    <div class="lnx-hero">
        <div style="position:relative; z-index:1;">
            <h1><i class="ph ph-chart-polar me-2"></i>OEE - Overall Equipment Effectiveness</h1>
            <p>Disponibilidad, Rendimiento y Calidad de tus máquinas</p>
        </div>
        <i class="ph ph-gauge hero-icon"></i>
    </div>

    <!-- ── OEE ── -->
    <div class="lnx-section-label">
        <div class="badge-label">
            <i class="ph ph-chart-polar"></i>OEE
        </div>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-lg-3 col-md-6">
            <div class="lnx-card">
                <div class="lnx-card-top">
                    <div class="lnx-icon-box lnx-icon-blue">
                        <i class="ph ph-gauge"></i>
                    </div>
                    <div>
                        <div class="lnx-card-title">OEE en Tiempo Real</div>
                        <div class="lnx-card-desc">Disponibilidad, Rendimiento y Calidad por turno</div>
                    </div>
                </div>
                <div class="lnx-card-footer">
                    <a asp-action="Oee" class="lnx-link-blue">
                        Ver OEE <i class="ph ph-arrow-right"></i>
                    </a>
                </div>
            </div>
        </div>
    </div>

</div>
